generator client {
  provider   = "prisma-client-js"
  engineType = "binary"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

model Navn {
  id        Int    @id @default(autoincrement())
  fornavn   String
  etternavn String
}
